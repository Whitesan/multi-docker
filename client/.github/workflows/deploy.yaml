name: Deploy MDocker
on:
  push:
    branches:
      - master # check your repo, your default branch might be master!

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      - run: docker build -t whitesani/react-test -f ./client/Dockerfile.dev ./client
      - run: docker run -e CI=true whitesani/react-test npm test

      - run: docker build -t whitesani/multi-client ./client
      - run: docker build -t whitesani/multi-nginx ./nginx
      - run: docker build -t whitesani/multi-server ./server
      - run: docker build -t whitesani/multi-worker ./worker

      - run: docker push whitesani/multi-client
      - run: docker push whitesani/multi-nginx
      - run: docker push whitesani/multi-server
      - run: docker push whitesani/multi-worker